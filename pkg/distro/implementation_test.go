package distro_test

import (
	"testing"

	. "github.com/smartystreets/goconvey/convey"
)

func TestCentOSImplementation(t *testing.T) {
	Convey("Given the distribution implementation of CentOS", t, func() {
		/*
		 *        mockExec := sys.NewMockExecutor()
		 *
		 *        centOS := distro.CentOS(mockExec)
		 *
		 */
		Convey("It should do consume the given meta-data", func() {
		})
	})
}
